# Source Asset Provenance

`urdf/widowGo1.urdf` and `meshes/` are copied byte-for-byte from `../Deep-Whole-Body-Control/legged_gym/resources/robots/widowGo1`. Only files referenced by the selected URDF are included. `source_manifest.sha256` records every imported source file and is verified before conversion.
